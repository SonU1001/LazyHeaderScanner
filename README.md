# LazyHeaderScanner
LazyHeaderScanner is a Python tool for passive reconnaissance. It audits HTTP headers to detect missing security controls (HSTS, CSP) and server info leaks. Features include bulk scanning, color-coded CLI output, and automated reporting. A lightweight solution for quick vulnerability assessment.
